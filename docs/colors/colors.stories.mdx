import { ColorPalette, ColorItem } from '@storybook/addon-docs';
import { Meta } from '@storybook/addon-docs';
import colors from './colors';

<Meta title="Styles/Colors" />

# Colors

The color palette


export const Palette = () => (
  <ColorPalette>
    {colors.map(({name,colors}) => (<ColorItem key={name} title={name} colors={colors} />))}
  </ColorPalette>
)

<Palette />
